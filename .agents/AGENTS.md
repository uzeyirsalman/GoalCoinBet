# Project-Scoped Behavioral Rules

## Pre-Commit and Pre-Deployment Verification
* **Local Server Testing Requirement:** Before committing any changes, and especially before deploying changes to the live web app, always spin up a local development server (e.g., using `npx http-server`) and provide the local URL so the user can test the updates first.
