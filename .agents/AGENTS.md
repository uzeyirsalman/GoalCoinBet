# Project-Scoped Behavioral Rules

## Pre-Commit and Pre-Deployment Verification
* **Local Server Testing Requirement:** Before committing any changes, and especially before deploying changes to the live web app, always spin up a local development server using `firebase serve` and provide the local URL (typically http://localhost:5000) so the user can test the updates first.
